{
  "name": "jf",
  "version": "1.0.0",
  "description": "",
  "main": ".eleventy.js",
  "config": {
    "dir": {
      "src": "src",
      "dist": "dist"
    }
  },
  "scripts": {
    "dev": "npm run watch:css && npm run eleventy --serve",
    "watch:css": "npm run build:css && npm run build:css -- --watch",
    "build:css": "node-sass $npm_package_config_dir_src/scss/*.scss -o $npm_package_config_dir_dist/css"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "markdown-it": "^8.4.2",
    "node-sass": "^4.12.0"
  }
}
